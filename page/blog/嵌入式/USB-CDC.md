# USB CDC：USB 虚拟串口

我们常使用 USB 通信，但是 USB 通信协议复杂，直接使用上手难，可移植性差。所以为了方便使用，USB 提供了进一层封装，如 CDC、HID 等。

CDC 在 USB 协议栈的更进一层，是实际提供功能对象，可以模拟串口的字节流。为了防止“粘包”等现象，还需要在字节流的基础上自定义包头、校验等（和在串口基础上自己写帧头等类似）。

## STM32 上的 USB CDC

